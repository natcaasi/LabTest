{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sin1"],
  "env": {
    "JWT_SECRET": {
      "description": "Secret key for JWT signing (generate with: openssl rand -hex 32)"
    },
    "RP_ID": {
      "description": "Relying Party ID for WebAuthn (your domain)"
    },
    "RP_NAME": {
      "description": "Relying Party Name for WebAuthn"
    },
    "RP_ORIGIN": {
      "description": "Relying Party Origin for WebAuthn (full HTTPS URL)"
    }
  }
}
